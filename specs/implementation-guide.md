# World Model Judge — Implementation Guide

Version 1.8 · 31 August 2026 · Bridges: all specs v1.8 (worlds v1.4) → `/gvm-build` · Requirements v1.2, Test Cases v1.5 (97 case IDs, 2 superseded)

> **Change note (v1.8 — design-review-008 repair, execution-verified).** Round 8 found the v1.7 pass had overstated its own "actually wired" claim a second time (BC-3 recurred a third round): TC-MU6-04, TC-MU6-05, TC-NF1-07, and TC-RP7-02 all had live case bodies but no chunk wiring, and the change note below had misattributed "is_baseline scope" to the pre-existing, unrelated **TC-MU5-02** instead of the real case, **TC-MU6-05** — now corrected. All four wired this round: TC-MU6-04/TC-MU6-05 → P3-C07; TC-NF1-07 → P1-C01 (plus TC-NF1-08, new, the sibling non-str collision the colon fix left open); TC-RP7-02 → P5-C04 (moved there with TC-JU12-04, which needs `results.html`, not built until P5-C04 — it was wrongly tagged at P4-C06). The phantom-gate enumeration corrected "nine"→"eleven" (TC-NF1-07/TC-MU6-04 were never added to the count). **The orchestration loop (cross-cutting ADR-004) is rewritten, not patched**: the v1.7 version's pseudocode didn't typecheck against the registry contract, its baseline ordering was backwards, and its one-region-per-`JudgeInput` choice contradicted the multi-region `region_labels` design stable since Round 3 — realised at P6-C01 as a whole-model-per-call design with an explicit baseline pre-pass; TC-NF1-06 is superseded by TC-NF1-09 (tombstoned, its claim was the inverted one). Purity harness hardening generalised (design-review-008 C1/C2/C3): `os.environ` patched at the class, not the instance; `numpy.random` patched at both module and private-submodule level; `numpy.datetime64`'s rebind (the one guard that structurally can't mutate in place) disclosed honestly. New gate TC-NF6-09 (models→judge/reporting) wired at P1-C03. See design-review-008.html.

> **Change note (v1.7 — design-review-007 repair, execution-verified).** Round 7's wiring findings closed, this time actually wired (Round 7 found four v1.6 change notes overstated their edits): **TC-JU12-03** (numpy C-clock phantom-gate) now in P4-C06's `[Test:]` tag and the "all nine" phantom-gate enumeration (was "eight"); **TC-MU7-02** (seed-purpose independence) now in P1-C01's tag; **TC-NF6-08** (models→harness import gate, new) in P1-C03; **TC-MU2-02** given full-scope wiring at P6-C01 (both worlds + competent models exist there); **TC-MU9-03** pinned to `cli.main(["list-models"])` so the argv routing is exercised (Round 7 showed the looser wording passes a broken subcommand table); the **orchestration loop** (cross-cutting ADR-004, new) realised at P6-C01 with TC-NF1-05/TC-NF1-06; new can-fail/doc cases TC-JU12-04 (purity residual), TC-MU5-02 (is_baseline scope — **design-review-008 found this was a misattribution; the real case is TC-MU6-05**), TC-MU6-04 (prereg content-hash), TC-NF1-07 (seed-key colon rejection), TC-RP7-02 (SVG byte-identity). See design-review-007.html.

> **Change note (v1.6 — design-review-006 repair, execution-verified).** Round 6's wiring/mechanism findings closed: TC-WD3-01's placement fixed (skeleton scope at P1-C03, full three-generator scope re-checked at P6-C01 where its generators exist); TC-NF3-01 given a real `[Test:]` tag at P6-C02 (was orphaned as "groundwork"); TC-MU2-01 moved to P4-C01 (P3-C02 had no judge refuse-logic); seed derivation is now `SeedSource` in `models.base` (P1-C01), with TC-NF1-04 (two-impl convergence) added; `is_baseline` set at P3-C02; `check_prereg`'s per-model presence check (TC-MU6-03) wired at P3-C07; **`wmj list-models`** added at P6-C01 so TC-MU9-03 is buildable in-process; the "nine chunks later" miscount in P2-C05's note corrected to twelve. See design-review-006.html.

> **Change note (v1.5 — design-review-005 repair).** Round 5 found three of the ten v1.4 re-wirings mis-placed and several new mechanisms unwired. Fixed at the level of the dependency graph: TC-JU9-03 moved from P4-C01 (no exceptions computation there) to P4-C04; TC-MU1-03 (reset-isolation) moved from P3-C03 (`direct` has no rollout state) to P3-C02 (`linear` does); P3-C01 and P5-C01 gained their previously-absent `[Test:]` tags; `baselines.py` ownership reconciled (created at P1-C02, extended at P2-C05/P3-C02 — the v1.4 "P2-C05 creates it" contradicted P1-C02's own Wiring Matrix row); P3-C03/04/06 now depend on P3-C07 for the pinned trajectory/epoch counts (the v1.4 "independent of C03–C06" left them with no source for those numbers); P5-C01/C03 now depend on P2-C05 (they extend its real `reporting/` files — the v1.4 plan let them race). New cases wired: TC-NF6-07 (models→worlds gate)→P1-C03, TC-JU12-02 (runtime-purity phantom)→P4-C06, TC-NF1-03 (seed no-shift)→P1-C01, TC-MU9-03 (CLI path)→P6-C01, TC-MU4-02 (fixture weight identity)→P3-C05, TC-RP-CARD-01 (model card)→P5-C04. **The critical path is re-derived to 10 chunks (was 9): the file-ownership and prereg-count edges Round 5 added lengthen the reporting branch, which is now the bottleneck — a real graph change, not a recount error.** See design-review-005.html.

> **Change note (v1.4).** Revised in the design-review-004 repair session. This guide was the document Round 4 (and the calibration record's BC-3 build check) showed had never been reconciled against the specs beyond arithmetic — so this pass reconciles it by rule: every test case in test-cases.md was greped against this guide's `[Test: ...]` tags and ten orphans were wired into their owning chunks (TC-NF6-02/03/04/06 → P1-C03; TC-MU1-03 → P3-C03; TC-JU9-03 → P4-C01; TC-JU3-01 → P4-C05 and P5-C03; TC-MU9-02 → P6-C01; TC-NF4-03, TC-NF5-02 → P6-C02). The Wiring Matrix's registry row now names TC-MU9-01's actual v1.4 mechanism (tmp-dir stub via a monkeypatched package `__path__` + production-path roster assertion + import-allowlist AST check, cross-cutting spec v1.4) instead of the git-diff mechanism two reviews ago rejected. The critical path is now three tied 9-chunk branches, not two — P2-C05 → P3-C02 is a hard prerequisite chain into P6-C01 per this guide's own dependency table, so "P2-C05 is off the critical path" was false. Phase 3's deliverable line said "eight registered contestants"; the roster has been seven in every spec since v1.0 (2 baselines + 2 unrigged + 3 fixtures) — the number was never recounted until this pass. See design-review-004.html and reviews/calibration.md.

> **Change note (v1.3).** Revised after `/gvm-design-review` design-review-003 (Round 3, dual/blind). Corrected the stated "critical path (11 chunks)" — it both miscounted its own named chunks (10, not 11) and omitted P3-C05, which the Dependency Network table itself names as a hard prerequisite of P6-C01; the corrected path has 9 chunks across two equal-length branches. No other chunk content changed — `fx-brittle`'s Round 3 redesign (models spec v1.3) removed the extra training-pipeline dependency Round 2's version would have needed, so P3-C05's existing dependency row required no change. See design-review-003.html for the full findings.

> **Change note (v1.2).** Revised after `/gvm-design-review` design-review-002 (Round 2, independent re-check). Panel D confirmed **P2-C05 itself is sound** — a genuine, demonstrable second thin vertical slice, correctly wired — but found two things wrong with this guide's own bookkeeping around it. First, **the chunk count is wrong: independently recounted twice against the Dependency Network table below, the true total is 28, not the "24" stated four times in this document since v1.1** (and, tracing the same error backward, the true pre-P2-C05 total was 27, not the "23" v1.0 stated — this miscount predates v1.1's own fix and was never caught until Round 2's strict, from-scratch recount). Corrected everywhere below; the v1.0/v1.1 changelog rows are left as the historical record of what was claimed at the time. Second, **the "P2-C05 shares no files with any Phase 3 chunk" parallel-safety claim was false**: P2-C05 and P3-C02 both target `wmj/models/baselines.py` per the models spec's own component design and this guide's Wiring Matrix naming. Fixed by stating explicitly that P2-C05 creates the file with the persistence baseline only, P3-C02 sequentially extends it, and removing the incorrect parallel claim. See design-review-002.html for the full findings.

**What this document is.** The build plan: six phases, twenty-eight chunks (design-review-002 correction — see change note above), each sized to one context window, each with its tests co-located, with the dependency network, the critical path, the parallelism map, and the wiring matrix proving every built module has a path into the running product.

**In plain words:** this is the order of work. Every chunk says what it builds, which spec section it reads, which test cases it must pass, and which later chunk would fail without it — so nothing gets built that nothing uses, and nothing gets used that nothing built.

---

## Build Phases

**MVP-1 check:** the first user-facing chunk is **P1-C02**, which delivers a runnable end-to-end product (`python -m wmj run --skeleton`: one world, one baseline, one skill score, one serialized output file, deterministic) — the smallest honest slice of the whole pipeline. **Second user-visible slice (design-review addition): P2-C05**, landing at chunk 8 rather than chunk 25 (design-review-002 correction: independently recounted from the Dependency Network table — the pre-fix plan's first further user-visible capability would have landed at chunk 25 of 27, not the previously-stated "chunk 19 of 23"; the position-8 claim for P2-C05 itself was always correct and is unaffected) — one real rendered chart (error-vs-horizon) from real LV data and a real baseline, the first artefact in the whole build that looks like what Dev (the named target user) actually reads. No exemption needed for either slice; the sequencing itself was the fix.

**Phase numbering:** starts at P1 — `build/handovers/` does not exist and no prior implementation guide exists in this repository (checked at write time).

### Phase 1 — Walking skeleton (deliverable: one command produces a deterministic skill report)

- **P1-C01 · Foundations.** `src` scaffold, `pyproject.toml` with the pinned two-package dependency set + lockfile, canonical serializer, **content-addressed seed derivation as `SeedSource`+`component_key` in `wmj/models/base.py`** (pinned exactly — blake2b digest_size=8 → one big-endian int; `purpose` key parts; cross-cutting ADR-002 rule 2 — design-review-006 moved it from `harness/seeds.py` so fixtures can import it without reaching the harness), single-thread env guard (asserts the three `*_NUM_THREADS` env vars are set before NumPy import — design-review-005, not a non-existent thread-introspection API), `errors.py` conventions. Tests: serializer canonical-bytes round-trip; **seed no-shift (TC-NF1-03)** and **two-implementation convergence of `component_key` from the pinned text (TC-NF1-04, design-review-006)**; thread-guard env-var assertion. *Spec: cross-cutting ADR-002/003, Dependency Budget.* [Test: TC-NF1-03, TC-NF1-04, TC-NF1-07 (seed-key colon rejection, design-review-007 — wired here, not previously in any chunk), TC-NF1-08 (seed-key non-str rejection, design-review-008 — the sibling type-representation collision the colon fix didn't close), TC-MU7-02 (seed-purpose independence: train/eval/benchmark start streams distinct by `purpose` key — design-review-007 wired here, the property lives on `SeedSource` built in this chunk), groundwork for TC-NF1-01] ~1 session.
- **P1-C02 · MVP slice (user-facing).** Minimal LV world (`transition` + shared RK4), **creates `wmj/models/baselines.py` with persistence + linear baselines** (mean rules + a minimal spread; real spread-fitting extended at P2-C05/P3-C02 — design-review-005 clarified this ownership), the harness's minimal `WorldContext`/`TrainingData` construction, judge `skill.py` (CRPS + skill), `python -m wmj run --skeleton` writing a `wmj-skeleton/0` JSON report. Acceptance: clean run from checkout; two consecutive runs byte-identical. *Spec: worlds §4.1/4.3, models ADR-M1/M2, judge ADR-J1.* [Test: TC-WD1-01 (LV half), TC-MU2-02 seed] ~1 session.
- **P1-C03 · Gates.** `tests/gates/`: the judge import-allowlist AST **lint** and its family (banned-identifier scan, wholesale `numpy.random` ban, the evasion-fixture regression corpus, the clean-pass guard, and the **models→worlds import gate TC-NF6-07**, the **models→harness import gate TC-NF6-08** (design-review-007 — the symmetric gate for the direction the `SeedSource`-in-`models.base` design depends on; same AST import-walk), and the **models→judge/models→reporting import gate TC-NF6-09** (design-review-008 — the same class of gap, undisclosed, for the two remaining sideways directions; `wmj/models/*`'s only sanctioned outward imports are now numpy/math/dataclasses/typing plus `wmj.models.base`/`wmj.models.registry`, nothing else, in any direction) — cross-cutting spec v1.8's restructured gate; the AST checks are fast lint, the primary purity tripwire is the runtime harness TC-JU12-01 built in P4-C06, with TC-NF1-01 byte-identity as the load-bearing backstop); WD-3 dt-alignment gate + its negative (mismatched dt must fail — design-review-005: the gate checks dt-alignment, not integrator-class identity, since no model owns an integrator, worlds ADR-W1); ten-run byte-identity harness over the skeleton output. *Spec: cross-cutting ADR-003, worlds ADR-W1.* [Test: TC-NF6-01, TC-NF6-02, TC-NF6-03, TC-NF6-04, TC-NF6-06, TC-NF6-07, TC-NF6-08 (models→harness gate — design-review-007), TC-NF6-09 (models→judge/reporting gate — design-review-008), TC-WD3-01 (skeleton scope: LV eval-truth integrator only — full three-generator identity re-checked at P6-C01, design-review-006: the case's benchmarks/training-data generators don't exist until P2-C03/P3-C06), TC-WD3-02 (dt-alignment scope), TC-NF1-01 (skeleton scope)] ~1 session.

### Phase 2 — Worlds complete (deliverable: both worlds + benchmark artefacts)

- **P2-C01 · LV full.** Regions, tasks, conserved V, clamp guard, action-range validation. [Test: TC-WD1-01, TC-WD2-01, TC-WD6-01, TC-WD6-02] ∥ *parallel with P2-C02.*
- **P2-C02 · Pendulum full.** EOM, energy, regions, tasks, unwrapped angles. [Test: TC-WD1-01, TC-WD2-01, TC-WD7-01, TC-WD7-02] ∥
- **P2-C03 · Divergence + drift artefacts.** Benchmark generator (64 starts/region, median curves), drift measurement vs the 1e-6 bound, curve sanity assertions. *Needs C01+C02.* [Test: TC-WD3-03, TC-WD4-01, TC-WD4-02]
- **P2-C04 · Region labelling.** In/out labelling with axis attribution, boundary determinism. *Needs C01+C02.* [Test: TC-WD5-01, TC-WD5-02] ∥ *parallel with P2-C03.*
- **P2-C05 · First real chart (user-facing, design-review addition).** A second, deliberately early thin vertical slice: **extends P1-C02's `wmj/models/baselines.py` with the persistence baseline's training-residual spread-fitting** (design-review-005 fix — the v1.4 text said this chunk *creates* `baselines.py`, contradicting P1-C02's Wiring Matrix row which already consumes it; P1-C02 creates the file with both baselines' mean rules, P2-C05 adds persistence's real spread fit, P3-C02 adds linear's) and **creates the minimal `reporting/style.py` + `reporting/horizon_plot.py`** (extended by P5-C01/C03, which therefore depend on this chunk — design-review-005: the v1.4 plan let P5-C01 run before P2-C05 and race on `style.py`), wiring it plus P1-C02's `skill.py`/CRPS to the LV world's real trajectories and P2-C03's divergence curve, then renders a scoped Chart 2 (error-vs-horizon, reporting ADR-R2) to `out/charts/lv-persistence-horizon.png`. **Scope disclosure (design-review-005 — the v1.4 acceptance demanded the *verbatim* ADR-R3 Chart-2 caption, which narrates the JU-6 switch step sourced from `climatology.per_task`, a Verdict field first computed at P4-C05, which is chunk 20 in the phase list to P2-C05's chunk 8 — **twelve chunks later**, not "nine" as v1.5 miscounted, design-review-006):** this chunk renders the error curve and a **scoped caption without the switch-step clause**; the switch-line feature and the full verbatim caption arrive with the full data at P5-C03. Acceptance: the rendered chart and its scoped caption exist and are non-empty. *Needs P1-C02, P2-C03.* [Test: TC-RP2-01 (partial scope: one model, one world, no switch line — extended to full scope in P5-C03)] ~1 session.

### Phase 3 — Models (deliverable: all seven registered contestants + prereg tooling)

- **P3-C01 · MLP core.** Forward/backward/Adam, gradient-check-first (the failing finite-difference test precedes backprop). *Spec: models ADR-M3 shared architecture.* [Test: gradient-check finite-difference to 1e-6 (models spec §8 — the MLP's own first test, pre-TC discipline; design-review-005 made this tag explicit rather than absent)] ∥ *parallel with P3-C02, P3-C07.*
- **P3-C02 · Baseline spreads.** **Extends `wmj/models/baselines.py`** (created at P1-C02, see design-review-005 note below; P2-C05 added persistence's spread-fitting, this chunk adds linear's) with the linear-extrapolation baseline's training-residual spread fitting and registry wiring for both baselines; both set `is_baseline=True` (models ADR-M1, design-review-006). Linear carries rollout-local state (the previous state — models ADR-M1), so this chunk owns the `reset()`-isolation test. [Test: TC-MU1-01/02 (baselines), TC-MU1-03 (reset() clears rollout state — design-review-005: moved here from P3-C03, since `direct` has no rollout-local state to leak but `linear` does)] ∥ *parallel with P3-C01, not with P2-C05.*
- **P3-C03 · Model A (direct).** Variance head, Gaussian NLL training (reads the pinned epoch count from `prereg/recipe.md`, committed by P3-C07). *Needs C01, C06, C07 (epoch count).* [Test: TC-MU1-01/02] ∥
- **P3-C04 · Model B (ensemble).** K=5, pre-registered point rule + `sqrt(1+1/K)`·std(ddof=1) spread mapping (reads the pinned epoch count from `prereg/recipe.md`, P3-C07). *Needs C01, C06, C07 (epoch count).* [Test: TC-MU5-02, TC-MU5-03] ∥ *parallel with C03.*
- **P3-C05 · Fixtures.** The three one-corruption wrappers, `is_fixture` flag; each builds a bit-identical `direct` core via the same content-addressed seed (models ADR-M4, design-review-005). *Needs C03.* [Test: TC-MU3-01/02/03 (behavioural halves), TC-MU4-01 (code surface), TC-MU4-02 (fixture inner net weight-identical to `direct`)]
- **P3-C06 · Training data + determinism.** `TrainingData` construction (the seeded trajectory dataset the factories receive — models ADR-M1, design-review-005), start-disjointness assertion, train-twice-identical test; reads the pinned trajectory count N from `prereg/recipe.md` (P3-C07). *Needs C01, P2-C01/02, C07 (N count).* [Test: TC-MU7-01, TC-MU8-01]
- **P3-C07 · Prereg tooling.** `derive_thresholds.py` (exact binomial → `prereg/thresholds.json`), `check_prereg` git-ordering checker; commit `prereg/recipe.md` (incl. the pinned trajectory count N and epoch count) + `prediction.md` + `thresholds.json`. **Must precede C03/C04/C06** (design-review-005 fix — the v1.4 "independent of C03–C06" was wrong: those chunks read N and the epoch count, which live only in `recipe.md`; the previous ordering left them with no source for those numbers). *Needs P1-C01.* [Test: TC-MU6-01, TC-MU6-03 (per-model prereg-entry presence — design-review-006), TC-MU6-04 (prereg content-invariance via `git log --reverse --diff-filter=A` — design-review-007, wired here — not previously in any chunk), TC-MU6-05 (is_baseline prereg-exempt but not a comparator — design-review-007, wired here after a change-note misattribution to the unrelated TC-MU5-02 left it unbuilt), TC-JU11-01, TC-JU11-02, TC-MU5-01 (margin read)] ∥ *parallel with P3-C01.*

### Phase 4 — Judge complete (deliverable: full verdicts from arrays)

- **P4-C01 · Types + verdict assembly.** `JudgeInput`, `Verdict`, limitations constants, refuse-on-missing-field. [Test: TC-JU1-01, TC-JU9-01, TC-JU9-02, TC-JU10-01, TC-MU2-01 (judge refuses on missing baselines — design-review-006: moved here from P3-C02, which has no judge verdict-assembly logic; the refuse-on-missing-field path is built in this chunk)]
- **P4-C02 · Skill (extend P1-C02).** Per-task/region skill vs both baselines; anti-gaming property test. [Test: TC-JU2-01, TC-JU4-02] ∥ *C02–C05 parallel after C01.*
- **P4-C03 · Calibration + sharpness.** Four-level coverage in/out region; width monotonicity property. [Test: TC-JU4-01, TC-JU5-01, TC-JU5-02] ∥
- **P4-C04 · Exceptions + bands.** Trial-wise counting, band assignment from thresholds data, hedging cross-flag; the `exceptions.observed` = sum-over-`trials.is_exception` invariant lives here (both blocks are computed in this chunk). *Needs P3-C07 format.* [Test: TC-JU8-01, TC-JU8-02, TC-JU8-03, TC-JU9-03 (the observed/is_exception reconciliation property — design-review-005: moved here from P4-C01, which builds only types and has no exceptions/trials computation to exercise the invariant against)] ∥
- **P4-C05 · Climatology + horizons.** Switch step, conditioned climatology (16 bins, re-measured invariant), trust horizons in dual units. *Needs P2-C03 artefact shape.* [Test: TC-JU3-01 (error-vs-horizon block, judge half), TC-JU6-01, TC-JU6-02, TC-JU7-01, TC-JU7-02] ∥
- **P4-C06 · Blindness/purity properties.** Label-swap invariance; the **runtime purity harness (TC-JU12-01)** — the primary purity tripwire (judge spec §4, cross-cutting ADR-002 rule 3, design-review-008): runs the judge's call graph with the *known* ambient-capability surface **mutated in place, at every level each object is independently bound** — `os`'s effectful functions including process-creation primitives (`fork`/`exec*`/`posix_spawn`, design-review-008 I10) + `os.environ`'s real **class** `_Environ` (design-review-008 C1 — Round 7's instance-level patch missed `os.environ["X"]`/`dict(os.environ)`; the class-level dunders close it), `builtins.open`/`io.open`, clock/network/subprocess, **every public callable in `numpy.random`, at both the top-level module and each private submodule that independently binds the same class** (design-review-008 C3 — Round 7's top-level-only patch missed `numpy.random._pcg64.PCG64` etc.), and **`numpy.datetime64`** (the one guard that is a disclosed rebind, not a mutation — design-review-008 C2, it is an immutable C type) — with its phantom-gate cases `TC-JU12-02` (a `__globals__`-routed `os` read) and `TC-JU12-03` (a `numpy.datetime64('now')` read) both proving the guard trips; no-learned-component static check. *Needs C01–C05.* [Test: TC-JU1-02, TC-JU12-01, TC-JU12-02 (phantom-gate), TC-JU12-03 (phantom-gate, numpy C-clock — design-review-007 wired here), TC-JU13-01]

### Phase 5 — Reporting (deliverable: charts + page from verdicts)

- **P5-C01 · Style + captions.** Extends P2-C05's minimal `reporting/style.py` to the full shared Matplotlib style, colour semantics, `mark_fixture`, and the caption templates. *Spec: reporting ADR-R1/R3/R4.* *Needs P2-C05 (shares `style.py`), P1-C01.* [Test: TC-RP8-01 (`mark_fixture` present in the figure), TC-RP5-01 (caption templates ≤3 sentences) — design-review-005 made these tags explicit] ∥ *C02–C04 parallel after C01.*
- **P5-C02 · Exception plot.** [Test: TC-RP1-01, TC-RP8-01] ∥ *C02–C04 parallel after C01.*
- **P5-C03 · Horizon + calibration plots.** Extends P2-C05's `horizon_plot.py` to every model/world/region (now with the world-time axis from `error_vs_horizon.dt` and the JU-6 switch lines from `climatology.per_task`, the full verbatim caption) and adds the calibration chart. *Needs P2-C05 (shares `horizon_plot.py`), P5-C01, P4-C01.* [Test: TC-RP2-01 (full scope), TC-RP3-01, TC-JU3-01 (rendered half: model curve and WD-4 reference on the same axes, same units)] ∥
- **P5-C04 · Comparison table + page + writer.** Table with disagreement marking (ordinary-error rank + its skill counterpart, design-review-005), `results.html` **including the model card** (the seven `limitations` + `not_tested`, reporting ADR-R5), verdict/manifest writer via the canonical serializer (design-review-008 C8: `wmj/reporting/writer.py` is the sole writer of every `out/` artefact — this chunk owns that write call, not the harness). [Test: TC-RP4-01, TC-RP4-02, TC-RP6-01, TC-RP-CARD-01 (model card present and legible, judged), TC-JU12-04 (purity-harness disclosed-residual doc case, design-review-007 — moved here from P4-C06 design-review-008 I9: its three-way consistency check needs `results.html`, which doesn't exist until this chunk), TC-RP7-02 (SVG byte-identity across processes, design-review-007 — wired here, where `wmj/reporting/style.py`'s savefig calls exist)] ∥

### Phase 6 — Integration closure and the judged run (deliverable: the published result)

- **P6-C01 · Full wiring.** `python -m wmj run`: the **orchestration loop pinned in cross-cutting ADR-004** (design-review-008 rewrite — Round 8 found the design-review-007 version broken: its `for model in all_models(): m = model.factory(...)` line didn't typecheck against the registry's `dict[str, Factory]` contract, its baseline ordering was backwards, and its one-region-per-`JudgeInput` choice contradicted the multi-region `region_labels` design stable since Round 3) — gates → benchmarks → training/load → an explicit **baseline pre-pass** (`persistence`/`linear` rolled out first, since they sort *last* in `all_models()`'s sorted-name order but every model's `JudgeInput` needs their predictions) → rollouts over `sorted(all_models().items())` (200 paired trials per region, every region a model's world declares handed to one call) → judging (**one pure call per (model, world)** — every region inside one `JudgeInput`, matching `region_labels`' existing per-trial shape and `JudgedResult`'s "one per (model, world)" contract, no per-region collation step) → reporting; `python -m wmj verify` byte-comparison mode; **`python -m wmj list-models`** — an internal, cheap command that prints the roster `harness.trials` assembles and exits (design-review-006: added so TC-MU9-03 can observe the CLI's real roster path without running the 600 s pipeline or tripping `ModelContractError`). Closes every seam left open by P2–P5; also re-checks the full three-generator WD-3 identity now that benchmarks and training-data generators exist (TC-WD3-01 full scope). [Test: TC-MU3-01/02/03 (judged halves), TC-MU4-01 (all surfaces), TC-MU9-01, TC-MU9-02 (phantom-gate), TC-MU9-03 (the CLI dispatch routes through `harness.trials` — invoked as `wmj.harness.cli.main(["list-models"])` so the argv→command routing is exercised, not a handler called directly; design-review-007 pinned the entry point after Round 7 showed the looser "entry function" wording passes even when the subcommand table is broken), TC-MU2-02 (full scope: both worlds, both baselines score below every competent model — design-review-007 wired here, where both worlds and the trained models exist), TC-NF1-05 (paired starts identical across models, per region — ADR-004), TC-NF1-09 (each `JudgeInput`'s `n_trials` = N × its world's region count, every declared region present — design-review-008, replaces TC-NF1-06's now-inverted "one region" claim, tombstoned), TC-WD3-01 (full three-generator scope), full-scope TC-NF1-01/02]
- **P6-C02 · Startup verification + NF gates.** Clean-checkout single-command test, wall-clock budget check (< 600 s, per the corrected ~16M-call envelope, judge ADR-J6), dependency-manifest gate, forbidden-terms scan (working tree + commit messages + all objects via `--batch-all-objects`, and the `core.hooksPath` wiring check — cross-cutting NF-4), and the **`.md`→`.html` spec-parity gate (TC-NF5-02)**. **The parity gate operates on HTML the build's own doc-generation step regenerates with the `generated-from` sha256 comment — not the hand-maintained twins (design-review-005 note — Round 5 found the currently-committed twins carry no such comment, so a gate run against them as-is would fail permanently; this chunk builds the doc-generation step that stamps the comment, and its gate runs against that output).** This chunk's acceptance test is the product startup verification. [Test: TC-RP7-01, TC-NF2-01, TC-NF3-01 (dependency-manifest gate — design-review-006: now a real tag here, not the "groundwork" mention at P1-C01), TC-NF4-01, TC-NF4-03, TC-NF5-02]
- **P6-C03 · The pre-registered judged run.** `check_prereg` certification → full pipeline on the unrigged models → publish `out/` → record the MU-6 either-way publication. Procedural checklist + mechanical certification. [Test: TC-MU5-01, TC-MU6-02 (judged), TC-JU10-02 / TC-RP5-01 / TC-NF4-02 / TC-NF5-01 (judged, human pass)]

---

## Dependency Network

| Chunk | Depends on | Enables | Parallel with |
|---|---|---|---|
| P1-C01 | — | everything | — |
| P1-C02 | P1-C01 | P1-C03, P2, P3, P4-C02 | — |
| P1-C03 | P1-C02 | (gate coverage for all) | — |
| P2-C01 | P1-C02 | P2-C03/04, P3-C06 | P2-C02 |
| P2-C02 | P1-C02 | P2-C03/04, P3-C06 | P2-C01 |
| P2-C03 | P2-C01, P2-C02 | P4-C05, P6-C01 | P2-C04 |
| P2-C04 | P2-C01, P2-C02 | P6-C01 | P2-C03 |
| P2-C05 | P1-C02, P2-C03 | P3-C02, P5-C01, P5-C03 (creates `baselines.py`'s persistence spread + minimal `reporting/style.py`+`horizon_plot.py` they extend) | every P3 chunk except P3-C02 |
| P3-C01 | P1-C01 | P3-C03/04 | P3-C02, P3-C07 |
| P3-C02 | P1-C02, P2-C05 | P6-C01 | P3-C01, P3-C07 |
| P3-C03 | P3-C01, P3-C06, P3-C07 | P3-C05 | P3-C04 |
| P3-C04 | P3-C01, P3-C06, P3-C07 | P6-C01 | P3-C03 |
| P3-C05 | P3-C03 | P6-C01 | P4-C01 |
| P3-C06 | P3-C01, P2-C01/02, P3-C07 | P3-C03/04 | — |
| P3-C07 | P1-C01 | P3-C03/04/06 (pinned counts), P4-C04, P6-C03 | P3-C01 |
| P4-C01 | P1-C02 | P4-C02..06 | P3-C05 |
| P4-C02..C05 | P4-C01 (+C04: P3-C07; +C05: P2-C03) | P4-C06, P6-C01 | each other |
| P4-C06 | P4-C01..05 | P6-C01 | P5-C01 |
| P5-C01 | P2-C05, P1-C01 | P5-C02..04 | P4-C06 |
| P5-C02 | P5-C01, P4-C01 (Verdict type) | P6-C01 | P5-C04 |
| P5-C03 | P2-C05, P5-C01, P4-C01 | P6-C01 | — (shares `horizon_plot.py` with P2-C05, so after it) |
| P5-C04 | P5-C01, P4-C01 (Verdict type) | P6-C01 | P5-C02 |
| P6-C01 | all P2–P5 | P6-C02/03 | — |
| P6-C02 | P6-C01 | P6-C03 | — |
| P6-C03 | P6-C02, P3-C07 | done | — |

**Two-track network (backend = worlds/models/judge; frontend = reporting):**

```
P1-C01 ─ P1-C02 ─ P1-C03
            │
   ┌────────┼──────────────┐
   ▼        ▼              ▼
P2-C01 ∥ P2-C02        P3-C01 ∥ P3-C02 ∥ P3-C07
   └───┬────┘              │                │
 P2-C03 ∥ P2-C04       P3-C06               │
   │   │                   │                │
   │   └─► P2-C05 ─► P3-C02  (first real chart; also creates reporting/style.py + horizon_plot.py)
   │           │           │                │
   │           │     P3-C03 ∥ P3-C04        │   (P3-C03/04/06 also need P3-C07's pinned counts)
   │           │           │                │
   │           │        P3-C05              │
   │           │           │                │
   └───────────┼─► P4-C01 ◄┘                │
   │           │   P4-C02 ∥ C03 ∥ C04 ∥ C05 ┘ (C04)
   │           │        │
   │           │     P4-C06
   │           ▼                     [reporting track — now the critical branch]
   │   P5-C01 ─► C02 ∥ C03 ∥ C04   (P5-C01/C03 extend P2-C05's real reporting files)
   └───────────┴──────────┬──────────────┘
                       P6-C01 ─ P6-C02 ─ P6-C03
```

**Critical path (10 chunks — design-review-005 re-derivation: the Round-5 fix that made P5-C01/C03 extend P2-C05's real `reporting/` files, and made P3-C03/04/06 depend on P3-C07 for the pinned counts, lengthened the longest chain by one and moved the bottleneck onto the *reporting* branch. Re-derived from scratch by longest-path over the current dependency table — `L(n) = 1 + max L(deps)`):** the dominant chain is **P1-C01 → P1-C02 → P2-C01 → P2-C03 → P2-C05 → P5-C01 → {P5-C02 ∥ P5-C03 ∥ P5-C04} → P6-C01 → P6-C02 → P6-C03** = 10 chunks, with the three P5 leaf chunks (all length 7) tied at the last pre-integration level. The backend branches are now *shorter* than the reporting branch and no longer critical: judge (P2-C03→P4-C05→P4-C06) and models (P3-C06→P3-C03→P3-C05, and P2-C05→P3-C02) each reach P6-C01 at length 6, one below the reporting branch's 7. **This is a real change, not a recount error:** the v1.4 "9 chunks, three tied branches" was correct *for the v1.4 dependency graph*; Round 5's file-ownership and prereg-count fixes added edges (P2-C05→P5-C01, P3-C07→P3-C0{3,4,6}) that the v1.4 graph did not have, and those edges lengthen the path. The 28-chunk total is unchanged.

---

## Wiring Matrix

| Entry point | Consumed modules | Wiring chunk | Demanded by |
|---|---|---|---|
| `wmj run --skeleton` (harness.cli) | `worlds.lv`, `worlds.integrator`, `models.baselines`, `judge.skill`, `harness.serialize` | P1-C02 | P1-C03 (ten-run byte gate consumes the skeleton output) |
| `wmj chart-preview` (harness.cli, design-review addition) | `worlds.lv`, `worlds.divergence` (P2-C03 artefact), `models.baselines` (persistence only), `judge.skill`, `reporting.style` (minimal), `reporting.horizon_plot` | P2-C05 | P2-C05's own acceptance test (rendered chart + caption exist); later demanded again, at full scope, by P5-C03 |
| `wmj run` (harness.cli) | `worlds.lv`, `worlds.pendulum`, `harness.benchmarks`, `harness.regions`, `models.registry` (baselines, direct, ensemble, fixtures), `harness.trials`, `judge.verdict` (skill, calibration, sharpness, exceptions, climatology, horizon, limitations), `reporting.*`, `harness.serialize` | P6-C01 | P6-C02 (clean-checkout startup acceptance test) |
| `wmj verify` (harness.cli) | full `wmj run` path + byte comparison | P6-C01 | P6-C02 (TC-RP7-01 acceptance) |
| `harness.derive_thresholds` | exact-binomial derivation, `harness.serialize` | P3-C07 | P4-C04 (band tests read the committed thresholds format) |
| `harness.check_prereg` | git-timestamp inspection | P3-C07 | P6-C03 (judged-run certification refuses without it) |
| `worlds.divergence` (producer) | consumed by `harness.benchmarks` → `judge.climatology`, `reporting.horizon_plot` | P2-C03, P6-C01 | P4-C05 (climatology tests demand the curve artefact shape) |
| `models.mlp` (producer) | consumed by `models.direct`, `models.ensemble` | P3-C03, P3-C04 | P3-C03 (direct's failing NLL-training test demanded the MLP) |
| `models.fixtures` (producer) | consumed by `harness.trials` roster | P6-C01 | P6-C01 (TC-MU3 judged-half acceptance runs fixtures through the full pipeline) |
| `judge.limitations` (producer) | consumed by `judge.verdict` | P4-C01 | P4-C01 (TC-JU10-01 field-presence test) |
| `reporting.captions` (producer) | consumed by all four chart modules | P5-C02..C04 | P6-C02 (startup acceptance asserts rendered captions exist per chart) |
| `reporting.style.mark_fixture` (producer) | consumed by every chart renderer | P5-C02..C04 | P6-C01 (TC-RP8-01 fixture-label acceptance) |
| `harness.serialize` (producer) | consumed by skeleton, thresholds writer, verdict writer, manifest | P1-C02, P3-C07, P5-C04 | P1-C03 (byte-identity gate is the serializer's failing consumer test) |
| `judge.types` (producer) | consumed by every judge module + reporting reader | P4-C01, P5-C04 | P4-C01 (TC-JU1-01 structural-blindness test) |
| `wmj.models.registry` (producer) | consumed by `harness.trials` discovery | P6-C01 | P6-C01 (TC-MU9-01 acceptance via `tests/gates/test_registry_isolation.py` — design-review-004 mechanism: a stub model written to a tmp dir grafted in via a monkeypatched `wmj.models.__path__`, discovered through the *production* `harness.trials` roster call, plus an import-allowlist AST check that no package outside `wmj/models/` imports a concrete model module — cross-cutting spec v1.4 ADR-003; supersedes both Round 2's git-diff mechanism and Round 3's write-into-the-real-package variant) |

No row has an empty wiring-chunk or `Demanded by` cell; no exemptions claimed.

---

## Claude-Specific Chunking

- **Context per chunk:** this guide's chunk entry + cross-cutting spec + the one relevant domain-spec section + the named TC-IDs. Never load all five specs into a build session.
- **Chunk size:** each chunk above is scoped to one session (~1–3 hours of agent work). If a chunk runs long, split by data variation (Cohn): e.g. P2-C01/C02 are already the worked example — same structure, different world.
- **Strict TDD:** the chunk's named TC-IDs are translated to failing pytest tests first; implementation follows. Negative/phantom-gate cases — all eleven: TC-WD3-02, TC-WD7-02, TC-NF1-02, TC-JU9-02, TC-JU11-02, TC-MU9-02, TC-NF6-04, TC-JU12-02, TC-JU12-03, TC-NF1-07, and TC-MU6-04 (design-review-005 added TC-JU12-02 — the runtime purity harness's own can-fail proof, a `__globals__`-routed `os` read the guard must catch; design-review-006 added TC-JU12-03 — the `numpy.datetime64` C-clock read the guard must also catch; design-review-007 added TC-NF1-07 (seed-key colon rejection) and TC-MU6-04 (prereg content-drift), both now actually wired at P1-C01/P3-C07 respectively — design-review-008 found the "nine" count here had never grown to include them; TC-NF6-06, the complementary clean-pass guard, rides in the same chunk as its gate) — are written as mutation tests in the same chunk as their gate.
- **Test co-location:** every chunk includes its tests; there are no "write tests" chunks anywhere in this guide.

## Parallel Work Identification (share-nothing)

Safe parallel sets (no shared files): {P2-C01, P2-C02} · {P3-C01, P3-C07} · {P3-C03, P3-C04} · {P4-C02, P4-C03, P4-C04, P4-C05} · {P5-C02, P5-C03, P5-C04} (the three P5 leaves own distinct chart modules; all three extend files created at P5-C01/P2-C05, so they run *after* those, not concurrently with them). **P2-C05 shares files with three later chunks and precedes all three** (design-review-005 correction — the v1.4 claim that P2-C05 only shares `baselines.py` with P3-C02 missed that P2-C05 also creates `reporting/style.py` and `reporting/horizon_plot.py`, extended by P5-C01 and P5-C03 respectively): it precedes **P3-C02** (`baselines.py`), **P5-C01** (`style.py`), and **P5-C03** (`horizon_plot.py`), and can run in parallel with every other Phase 3 chunk. Interface contracts at the boundaries are the specs' own (MU-1 Prediction, the divergence artefact JSON, the Verdict schema). Merge strategy: sequential merge in chunk-ID order within each parallel set; parallel chunks never modify the same file (each owns its module + its test file).

## Integration Closure

Deferred seams and their closing chunks: model training deferred from P3 rollout wiring → closed by P6-C01; chart rendering against real verdicts (P5 builds against fixture verdict records) → closed by P6-C01; runtime/dependency/confidentiality gates → P6-C02; prereg certification → P6-C03. A scan of this guide for "deferred"/"wired later" phrases resolves each to one of those three chunks. P6-C02 is the product startup verification chunk.

---

## Changelog

| Version | Date | Change |
|---|---|---|
| 1.0 | 2026-08-25 | Initial version. Starts at P1 — no prior build phases or implementation guide exist in this repository. 6 phases, 23 chunks, MVP-1 satisfied by P1-C02, wiring matrix complete with no exemptions. |
| 1.1 | 2026-08-25 | Design-review fix (design-review-001): added P2-C05, a second thin vertical slice (one real chart from real data) landing at chunk 8 rather than the previous plan's first further user-visible capability at chunk 19 (this "23"/"19" arithmetic was itself later found wrong in design-review-002 — see v1.2 row) — Phases 2–5 had otherwise reverted to a fully horizontal, layer-by-layer build after P1-C02, which the guide's own MVP-1 rule exists to forbid. 6 phases, "24" chunks (later corrected). |
| 1.2 | 2026-08-30 | Design-review fixes (design-review-002, Round 2): corrected the chunk count everywhere in this document (true total 28, not 24; true pre-P2-C05 total 27, not 23 — an arithmetic error present since v1.0 and never caught until an independent recount); fixed the false "P2-C05 shares no files with any Phase 3 chunk" parallel-safety claim (both target `wmj/models/baselines.py`) by making P3-C02 sequentially extend P2-C05's file instead; corrected P2-C05's acceptance criterion (Chart 2's caption template has no numeric slots to substitute); named the concrete `test_registry_isolation.py` mechanism for TC-MU9-01. |
| 1.3 | 2026-08-30 | Design-review fix (design-review-003, Round 3, dual/blind): corrected the critical-path line — 9 chunks across two equal-length branches, not "11" (which both miscounted its own list and omitted P3-C05). |
| 1.4 | 2026-08-31 | Design-review-004 repair session (full reconciliation, not arithmetic-only): wired ten orphaned test cases into their owning chunks; Wiring Matrix registry row updated to TC-MU9-01's v1.4 mechanism (tmp-dir stub + monkeypatched `__path__` + production-path roster assertion + import-allowlist); critical path corrected to three tied branches (P2-C05 → P3-C02 was always a prerequisite chain into P6-C01); "eight registered contestants" corrected to seven; phantom-gate list completed (7 cases, count stated). |
| 1.5 | 2026-08-31 | Design-review-005 repair: three of the ten v1.4 wirings re-placed (TC-JU9-03→P4-C04, TC-MU1-03→P3-C02, and the P2-C05 caption/`baselines.py`-ownership contradictions fixed); P3-C03/04/06→P3-C07 (pinned counts) and P5-C01/C03→P2-C05 (shared reporting files) dependencies added; six new cases wired (TC-NF6-07, TC-JU12-02, TC-NF1-03, TC-MU9-03, TC-MU4-02, TC-RP-CARD-01); P3-C01/P5-C01 gained `[Test:]` tags; phantom-gate list → 8 cases; **critical path re-derived to 10 chunks (reporting branch now dominant) — a genuine graph change from the new edges, not a recount error**; TC-NF5-02 clarified to run against build-regenerated HTML. |
| 1.6 | 2026-08-31 | Design-review-006 repair: TC-WD3-01 placement fixed (skeleton@P1-C03, full@P6-C01); TC-NF3-01 given a real tag @P6-C02 (was orphaned); TC-MU2-01 → P4-C01; `SeedSource` in `models.base` @P1-C01 with TC-NF1-04; `is_baseline` @P3-C02; TC-MU6-03 (per-model prereg) @P3-C07; `wmj list-models` @P6-C01 makes TC-MU9-03 buildable in-process; "nine chunks later" miscount → twelve. |
| 1.7 | 2026-08-31 | Design-review-007 repair: TC-JU12-03 → P4-C06 phantom-gate list; TC-MU7-02 → P1-C01; TC-NF6-08 → P1-C03; TC-MU2-02 full scope → P6-C01; TC-MU9-03 pinned to `cli.main(["list-models"])`; orchestration loop (cross-cutting ADR-004, v1) realised at P6-C01 with TC-NF1-05/06. (Changelog row added in v1.8 — design-review-008 found v1.7 stamped the banner and change note but never added this table row, the same gap Round 8 fixed for architecture-overview.md/reporting.md's own v1.6 rows.) |
| 1.8 | 2026-08-31 | Design-review-008 repair (execution-verified): TC-MU6-04/TC-MU6-05 → P3-C07, TC-NF1-07/TC-NF1-08 (new) → P1-C01, TC-RP7-02 + TC-JU12-04 (moved from P4-C06) → P5-C04 — four v1.7 orphans actually wired this time, TC-MU5-02 misattribution corrected; phantom-gate count "nine"→"eleven"; ADR-004 rewritten (whole-model-per-call, baseline pre-pass, registry-contract-correct) and realised at P6-C01, TC-NF1-06 superseded by TC-NF1-09 (tombstoned); TC-NF6-09 (models→judge/reporting gate) → P1-C03; purity harness generalised to patch every reachable binding, not just the first found. |

---

*Developed using the Grounded Vibe Methodology*
